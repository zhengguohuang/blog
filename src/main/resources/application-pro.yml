
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?
         useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: none #不要变动表结构
    show-sql: true
  messages:
    encoding: UTF-8
logging:
  level:
    root: warn
    tech.turl: info
  file:
    name: /data/log/blog-pro.log
server:
  port: 8088
comment:
  avatar: /images/avatar.png